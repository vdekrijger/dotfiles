{
  "name": "proof-capture",
  "version": "1.0.0",
  "description": "Deterministic artifact capture for the proof-dev pipeline (headless/autonomous mode)",
  "main": "dist/index.js",
  "bin": {
    "proof-capture": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "playwright": "^1.44.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "ts-node": "^10.9.0"
  },
  "engines": {
    "node": ">=20"
  }
}
